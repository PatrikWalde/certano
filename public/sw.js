// Service Worker für Certano PWA - TEMPORÄR DEAKTIVIERT
// Der Service Worker wird temporär deaktiviert, um Probleme mit dem Caching zu vermeiden

console.log('Service Worker: Deaktiviert - keine Caching-Funktionen aktiv');

// Alle Event Listener sind deaktiviert
// self.addEventListener('install', ...);
// self.addEventListener('activate', ...);
// self.addEventListener('fetch', ...);

// Service Worker bleibt registriert, aber macht nichts